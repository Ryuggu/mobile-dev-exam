<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ChatApp.ApiData"
             BackgroundColor="#B00A0C">
    
    <ListView x:Name="apiContactsListView" Margin="0, 8">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Grid>
                        <Label Text="{Binding Name}" TextColor="White" Margin="8,0,0,0" FontSize="12"/>
                        <Label Grid.Column="1" Text="{Binding Username}" TextColor="White" Margin="8,0,0,0" FontSize="12"/>
                        <Label Grid.Column="2" Text="{Binding Website}" TextColor="White" Margin="8,0,0,0" FontSize="12"/>
                        <Label Grid.Column="3" Text="{Binding Address.City}" TextColor="White" Margin="8,0,0,0" FontSize="12"/>
                        <Label Grid.Column="4" Text="{Binding Company.Name}" TextColor="White" Margin="8,0,8,0" FontSize="12"/>
                    </Grid>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>


</ContentPage>